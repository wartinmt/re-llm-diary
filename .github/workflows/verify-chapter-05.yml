name: Verify chapter 05

"on":
  push:
    paths:
      - "chapters/05-adaptive-router/**"
      - ".github/workflows/verify-chapter-05.yml"
  pull_request:
    paths:
      - "chapters/05-adaptive-router/**"
      - ".github/workflows/verify-chapter-05.yml"

permissions:
  contents: read

jobs:
  offline-tests:
    name: ${{ matrix.os }} / Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    env:
      PYTHONUTF8: "1"
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.10", "3.13"]

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: chapters/05-adaptive-router/requirements.txt
      - name: Install dependencies
        run: python -m pip install -r chapters/05-adaptive-router/requirements.txt
      - name: Syntax check
        working-directory: chapters/05-adaptive-router
        run: python -m py_compile main.py providers.py memory.py router.py
      - name: Unit tests
        working-directory: chapters/05-adaptive-router
        run: python -m unittest discover -s tests -v
      - name: Offline self-test
        working-directory: chapters/05-adaptive-router
        run: python main.py --self-test
