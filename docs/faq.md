# 常见问题

## 这是最终 Runtime 的源码吗？

不是。这里保存的是研究日记对应的阶段性实验和可运行示例。正式 Runtime 将使用独立仓库维护。

## 为什么从 DeepSeek 开始？

因为它是这段研究过程的真实起点，也适合用较低成本理解 API、上下文、Token 和模型调用。

## 每一章都可以独立运行吗？

原则上可以。每章目录会包含自己的 README、依赖说明和配置模板。

## 为什么不用一个现成的 Agent 框架？

这个系列会先从最小组件开始，逐步理解框架解决了什么问题、又带来了哪些限制，再决定哪些能力应该吸收。
