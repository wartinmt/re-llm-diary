# 第 10 章验证报告

## 状态

Verified Offline - 已完成源码、65 项单元测试、四个离线演示、文档与部署包验证；未调用模型 API 或真实外部工具。

## 已验证

- manifest 类型、风险、能力和输入字段校验；
- 发现阶段只读取文件，不导入插件；
- Python AST 类、方法、导入和明显危险调用检查；
- manifest 与实现不一致时拒绝；
- probe / preview 在独立进程和临时目录中运行；
- probe / preview 留下文件副作用时拒绝；
- 副作用工具需要显式确认；
- 准入记录原子保存；
- manifest 或源码变化后状态变为 stale；
- 每轮只询问一个缺失输入；
- 稳定计划键与持久执行回执；
- 相同计划重复提交不重复写文件；
- Windows UTF-8 工作流配置；
- 压缩包不包含 `.env`、`data/`、缓存或字节码。

## 边界

- 本章不是安全沙箱；
- AST 规则无法证明插件无恶意；
- 子进程和临时目录不能阻止所有系统访问；
- 示例只运行仓库内的本地教学插件；
- 不应将本章机制直接用于执行未知来源代码。
