# 验证清单

## 离线

- [ ] `python3 -m unittest discover -s tests -v` 全部通过；
- [ ] `python3 main.py --self-test` 通过且不联网；
- [ ] `--check-config` 不显示 API Key；
- [ ] `--check-memory` 能拒绝损坏存档；
- [ ] `--check-router` 能拒绝旧版本或损坏的成本记录；
- [ ] `/route` 前后正式记忆、Token 和累计成本均不变化；
- [ ] 三种策略的解释包含预计费用与预算分；
- [ ] 预算不足时验证计划明确关闭。

## 真实 API

- [ ] 成功回答后 `/costs` 增加 API 返回的输入和输出 Token；
- [ ] 缓存字段存在时能正确拆分命中与未命中；
- [ ] 没有缓存字段时全部输入按未命中计算；
- [ ] 主回答、验证和比较费用分栏累计；
- [ ] `VERIFY_MODE=auto` 不会对每个短问题都验证；
- [ ] `VERIFY_MODE=on` 在双模型和预算允许时启动验证；
- [ ] `PASS` 保留主答案；
- [ ] 完整 `REVISE` 替换主答案；
- [ ] 不完整 `REVISE` 与未知首行降级为 `UNCERTAIN`；
- [ ] 验证失败不删除已经成功的主答案；
- [ ] 所有主候选失败时不写入半轮正式记忆；
- [ ] `/compare` 不写入正式记忆，但会增加比较成本。

## 隐私与发布

- [ ] `.env` 未提交；
- [ ] `conversation.json` 不含 Key；
- [ ] `router_state.json` 不含问题或回答正文；
- [ ] 默认价格已经与发布当天官方页面复核；
- [ ] 文档明确说明本地估算不替代平台账单；
- [ ] 公开截图已遮住账户、路径和私人输入。
