# 第 04 章实验记录

## 配置状态

| 项目 | 结果 |
|---|---|
| `/models` 同时显示 DeepSeek 与 GLM | |
| 默认模型 | |
| 记忆文件位置 | |

## 手动切换

使用同一个问题分别普通对话：

> 一个会长期记住用户的助手，最容易犯什么错误？

| 模型 | 回答关注点 | 是否写入记忆 |
|---|---|---|
| DeepSeek | | 是 |
| GLM | | 是 |

## 临时比较

输入：

```text
/compare 一个会长期记住用户的助手，最容易犯什么错误？
```

| 模型 | 回答关注点 | 大致耗时 | 是否写入记忆 |
|---|---|---:|---|
| DeepSeek | | | 否 |
| GLM | | | 否 |

## 观察

1. 两个模型是否收到相同的历史与问题？
2. 切换模型后，旧对话是否仍然可用？
3. 比较完成后，`/history` 是否保持不变？
4. 一个模型失败时，另一个结果是否仍能显示？

## 一句话结论

> 这一章把模型从程序里的固定身份，改造成了 ________________________________。
