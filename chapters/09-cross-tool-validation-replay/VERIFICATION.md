# 第 09 章验证报告

## 状态

Verified Offline - 已完成源码、67 项单元测试、四个离线演示、文档和部署包验证；未调用真实 API 或外部工具。

## 已验证

- Python 3.10 兼容语法；
- 不可变计划保存与哈希；
- 文档和索引工具的幂等回执；
- 文档存在性与真实内容哈希验证；
- 标题、路径、计划哈希和磁盘哈希的联合验证；
- 组合验证失败时不写完成标记；
- 工具完成、Runtime 未保存回执时的查询恢复；
- 回滚也会先查询遗漏的工具回执，不会把仍存在的副作用标为已回滚；
- workflow ID 路径限制与执行前计划哈希核对；
- 已确认步骤不会重复执行；
- 逆依赖顺序补偿；
- 补偿动作自身幂等；
- 追加式事件序号与哈希链；
- 只读重放不修改流水账；
- Windows UTF-8 工作流配置；
- 压缩包不包含 `.env`、`data/`、缓存或真实密钥。

## 边界

- 示例工具只操作本地文件和 JSON 索引；
- 哈希链不是数字签名，不能抵抗拥有完整写权限的恶意重算；
- 补偿不保证恢复历史原样，只执行尽量抵消影响的新动作；
- 真实跨服务工作流还需要平台事务语义、权限、速率限制和人工升级策略。
